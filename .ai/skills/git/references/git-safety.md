# Git Safety

Always inspect `git status` and relevant diffs before destructive Git commands. Preserve uncommitted user work. Prefer new commits or branches over history rewriting when collaboration is involved. Never use a force operation merely to make a command succeed.
