# Query Safety

Bound mutation predicates. Preview affected rows before destructive changes. Use transactions where appropriate. Inspect query plans for material performance changes. Avoid accidental full-table updates or deletes. Treat user-provided sort and filter fields as controlled inputs.
