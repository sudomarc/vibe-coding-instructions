# Secure Coding Checklist

Validate untrusted input. Use parameterized database access. Encode output appropriately. Enforce authorization server-side. Limit privileges. Keep secrets out of source and logs. Use secure randomness for security-sensitive tokens. Avoid custom cryptography. Fail closed when authorization cannot be established. Log security-relevant events without leaking sensitive data.

Verify dependency and configuration changes when they alter attack surface.
