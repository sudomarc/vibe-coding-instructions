# Example API Change

Add an optional `cursor` query parameter to an existing listing endpoint. Preserve default behavior, document cursor semantics, validate malformed tokens, and add tests covering first page, next page, final page, and invalid cursor.
