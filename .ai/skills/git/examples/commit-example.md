# Example Commit

`fix(orders): preserve cursor scope during pagination`

The body explains the bug, the reused validator, and the focused regression test. The commit does not include unrelated formatting changes.
