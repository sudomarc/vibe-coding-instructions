# API Versioning

Prefer backwards-compatible additions when possible. Treat removals, renamed fields, semantic changes, and stricter validation as potentially breaking. Follow the repository's established versioning mechanism rather than inventing a second scheme.
