# Example Component Change

Add a disabled loading state to an existing submit button.

Inspect the current button component and form state first. Reuse existing loading indicators. Preserve accessible name and focus behavior. Add a focused test for disabled behavior and a manual/browser check for visual feedback.
