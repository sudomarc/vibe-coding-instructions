# Service Checklist

Inspect input validation, authorization, transaction boundaries, error mapping, retries, idempotency, logging, metrics, timeouts, dependencies, concurrency, shutdown behavior, and tests.

Keep infrastructure errors distinguishable from domain errors where that distinction affects retry or user-visible behavior.
