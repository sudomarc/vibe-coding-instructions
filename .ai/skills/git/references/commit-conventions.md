# Commit Conventions

When the repository uses Conventional Commits, use a type such as `feat`, `fix`, `docs`, `refactor`, `test`, `build`, `ci`, `perf`, or `chore` with a concise subject.

A good commit describes one coherent change. Include a body when the why is not obvious. Do not claim verification that did not occur.
