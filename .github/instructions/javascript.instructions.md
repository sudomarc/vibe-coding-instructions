---
applyTo: "**/*.js,**/*.jsx,**/*.ts,**/*.tsx"
---

Inspect the repository's runtime, package manager, formatter, linter, type-checking, and test conventions before editing JavaScript or TypeScript. Preserve existing module and error-handling patterns. Run focused checks after changes and use browser verification for user-visible frontend changes when available.
