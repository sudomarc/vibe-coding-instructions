# Prompt Patterns

A durable coding-agent prompt should define role, observable goal, verified context, constraints, tools, workflow, output contract, verification, and stop conditions.

Prefer:

`Inspect → plan → implement → verify → review`

over vague instructions such as “make it production ready”.

Use examples only for formats or decisions that are otherwise difficult to infer. Keep examples separate from repository facts.
