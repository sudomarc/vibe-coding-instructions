# Browser Verification

When browser tooling is available, verify the affected route loads without console errors, the intended interaction works, important loading/error states render, keyboard interaction is possible, and the final visual state is consistent with the requirement.

A browser screenshot is supporting evidence, not a substitute for tests.
