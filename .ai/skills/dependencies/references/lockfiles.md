# Lockfile Rules

Treat the manifest and lockfile as one change. Use the repository's package manager to update lock state. Inspect unexpected transitive changes. Do not hand-edit lockfiles unless the project's documented tooling requires it.

Verify clean install, build, and relevant tests after meaningful dependency updates.
