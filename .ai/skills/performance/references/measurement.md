# Performance Measurement

Record workload, environment, metric, baseline, sample count or duration when relevant, and comparison method. Avoid benchmarks that cannot reproduce the important workload. Treat noisy measurements cautiously.
