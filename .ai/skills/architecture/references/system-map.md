# System Mapping Guide

Map only the components relevant to the task.

For each boundary, record owner, inputs, outputs, state, failure modes, and dependency direction. Identify synchronous versus asynchronous flow, persistent versus ephemeral state, and trust boundaries.

Use repository search to verify the map. Do not infer architecture solely from directory names.
