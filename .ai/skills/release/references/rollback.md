# Rollback

Define rollback per layer: code, configuration, database schema, data transformation, and infrastructure. Record cases where rollback is not fully reversible and a forward fix is required.
