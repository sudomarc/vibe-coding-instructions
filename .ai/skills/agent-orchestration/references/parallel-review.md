# Parallel Review

Parallel review is useful when review dimensions are independent and the tool host can safely isolate them. Run read-only analyses in parallel. Avoid concurrent edits to the same files. Aggregate findings, validate high-impact findings against repository evidence, then apply fixes serially.
