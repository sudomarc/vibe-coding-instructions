# Example Release Plan

Build artifact, run full verification, deploy application code, apply backwards-compatible migration, monitor health, then enable the feature flag. Keep the flag available for containment. Verify core user journeys after rollout.
